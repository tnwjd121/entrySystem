수정 사항(2025-01-02)
- 날짜 페이지 수정 => 완료
- 통신사 선택 시 키보드 안 올라오게 (div인데 안 움직임) => 완료
- 이전 출입일자 불러오기 시 일자 연동 => 완료
- 휴대폰 번호 11자리(등록 페이지 변경) => 완료
- 날짜, 시간 불러오기 후 처음 클릭했을때는 첫번째여서 오늘 날짜로 세팅이 됨, 변경 안 되도록 변경 => 완료
- 출입 등록 수정 페이지 작성 => 완료
- list에서 전화번호(-) 넣어서 나오게 하기 => 완료
- 날짜, 시간 스크롤 스무스하게 변경
- 신청 완료페이지, 오류페이지, 관리자 문의 페이지 만들기
- 알림창 효과


prevButton에서 눌렀을 경우 count 별개
entryregistration에서 add, edit, prev 세가지 기준으로 세팅
setTypeOfSubmit
typeOfSubmit

휴대폰 번호 
앞에 2글자가 01일 경우 3-4-4(11)
앞에 2글자가 02일 경우 2-4-4(10) 또는 2-3-4(9)
앞에 2글자가 01,02가 아닌 경우 3-3-4(10), 3-4-4(11)

수정 사항(2025-01-03)
1. 출입 등록시 (고객사명 제거)
2. 본인인증 연락처 기준으로 key값 설정하여 리스트 가져오기
3. 플러스 버튼 누르면 티켓에 메인 출입자 정보 등록(전부 다 이름, 전화번호 가져오게 하기)
   => 등록 순서 : 협력업체, 직위, 성명, 연락처, 출입일시, 목적
4. 출입등록현황에서는 티켓으로 묶이게 하기
티켓 1 예시)
                               삭제
출입자 : 업체 이름 [외 ? 명]
출입목적 : ~~~~[ 등]
출입일시 :
인원추가버튼   수정버튼  삭제버튼   스크롤 아이콘
=> 대괄호는 인원이 추가 될 경우에만 넣기
=> 인원추가버튼 누르면 "+ 출입일시/목적 불러오기" 버튼 반영되어야함
=> 스크롤 아이콘 누르면 추가된 출입자에서 대해서 정보 나옴
=> 추가된 출입자의 경우 우측 위에 -버튼 넣어서 삭제하기(수정x)

※ 고객 등록 목서버 생성 필요함

2025-01-05
entrylist를 등록할때 전화번호가 key 값이 되게하기
entrycount 필요 없음
entryregistration에 설정 완료

수정해야되는거
- 처음 로그인시 이미 있는 전화번호로 들어왔을경우 새로 추가 x 안되고 해당하는 정보로 들어가기
- addlist 문제점 해결(속도가 느려서 전 데이터 보여줌_timeout 걸기, 상세보기 동시에 열 수 있도록 하기)
- addlist가 없을 경우 FaAngleDown 안보이게 하기

1. entry main조인으로 데이터 가져왔을때, 버튼 각각 클릭되게 하기
2. 처음 로그인 시 이미 있는 전화번호 들어왔을 경우 새로 추가 x
3. 티켓 위처럼 나오게 변경
4. 리스트 해당하는 전화번호만 나오게 하기
5. 스크롤 뻑뻑한지 확인할 필요 있음


  // 연락처 단위 조정 (전화번호를 제외해서 따로 빼둠)
  function formatPhoneNumber(phonenumber) {
    let formatphonenumber;
    if(phonenumber.slice(0,2)==="01"){
      formatphonenumber = `${phonenumber.slice(0,3)}-${phonenumber.slice(3,7)}-${phonenumber.slice(7,12)}`
    }else if(phonenumber.slice(0,2)==="02"){
      if(phonenumber.length===10){
        formatphonenumber = `${phonenumber.slice(0,2)}-${phonenumber.slice(2,6)}-${phonenumber.slice(6,11)}`
      }else if(phonenumber.length===9){
        formatphonenumber = `${phonenumber.slice(0,2)}-${phonenumber.slice(2,5)}-${phonenumber.slice(5,10)}`
      }
    }else{
      if(phonenumber.length===11){
        formatphonenumber = `${phonenumber.slice(0,3)}-${phonenumber.slice(3,7)}-${phonenumber.slice(7,12)}`
      }else if(phonenumber.length===10){
        formatphonenumber = `${phonenumber.slice(0,3)}-${phonenumber.slice(3,6)}-${phonenumber.slice(6,11)}`
      }
    }
    return formatphonenumber;
  }





